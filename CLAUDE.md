@AGENTS.md

## Claude Code

- `AGENTS.md` is the canonical project guidance for this repository. It covers the layout, the TEA flow, Fennel conventions, tests, and the checks to run before finishing.
- Keep this file short. Add shared project rules to `AGENTS.md`; put a rule here only when it applies to Claude Code alone.
- Use the scratchpad directory from the system prompt for temporary files. Do not write scratch files into the repository.
- The user handles git. Do not commit, stage, or inspect the working tree to review your own changes unless asked.
